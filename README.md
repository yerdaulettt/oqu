# Oqu

Online learning REST API backend using Go, PostgreSQL, Docker, Nginx, JWT. Project layout organized in repository pattern for better maintainability and separating data access logic. There are currently admin and user roles to make better authorization.

This command will run the project on localhost:8081
`docker compose up --build -d`